# Get this from your Supabase project: Settings -> Database -> Connection string (URI, "Transaction" pooler)
DATABASE_URL="postgresql://postgres.tomiypmejtvkplmoojpe:!jD6Vt&bZ?*zny5@aws-1-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Any long random string. Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="replace-this-with-a-long-random-string"

# How long a login session token stays valid
JWT_EXPIRES_IN="7d"

# The URL your frontend runs on (for CORS). Add your Vercel URL later too.
FRONTEND_URL="http://localhost:5173"

PORT=4000
